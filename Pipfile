[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
pytest = "==7.1.2"

[requires]
python_version = "3.8"

[scripts]
compose-up = "docker-compose up -d --build"

compose-down = "docker-compose down"